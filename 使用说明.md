# ManualDock v0.2.0 使用说明

ManualDock 用于导入已登录设备的在线手册与 API 规范，采集结果保存在本机，供离线阅读和 AI 检索。无需提供账号密码；工具不会读取 Cookie、Token 或密码。

## 安装与更新

在 Chrome 打开 `chrome://extensions`，开启开发者模式，加载本工程的 **extension** 文件夹，名称应为 **ManualDock**。保留固定安装目录，更新时替换文件并点“重新加载”；换一个路径加载会创建独立的扩展身份和进度。清除浏览器数据或卸载前先导出备份。

配套 Skill 名为 `import-security-manuals`。安装 Skill 不等于安装 Chrome 扩展。如果只使用 Skill 包，加载 `skill/import-security-manuals/assets/extension`；它与工程 `extension` 是两个安装来源，选择其中一个即可。

## 导入网页手册

1. 自行登录设备，打开手册并点击扩展。保留原手册标签页和控制页。
2. 检查识别出的目录及正文。识别不准时，点选目录区域、正文区域和两个不同示例章节，点击“验证两章并保存规则”。点选只记录目标位置，按 Esc 可取消。
3. 填写产品和版本，点击“开始 / 继续导入”。规则、产品、版本和手册入口共同区分任务；重新打开后选择原任务，避免把另一份手册接到旧队列。
4. 检查失败、待核对和目录警告。导出 Markdown 供 AI 读取文字，离线 HTML 阅读图片与表格，完整 JSON 用于保存档案，采集报告记录缺口。

已发现条目按稳定章节编号去重，同网址不同章节使用目录路径和可靠标识。正文与另一章相同会标为待核对，不能拿上一章正文充数。但没有独立的完整目录可对账，因此“处理完已发现章节”不代表整册无遗漏。每任务最多 5,000 章；未加载目录、虚拟目录和无法访问的页面结构需要进一步校准或适配。

## 断线后每三分钟自动继续

每章完成后写入本地数据库。遇到可重试的网络、登录或加载错误，工具记录恢复状态，等待 **3 分钟**再尝试；已保存章节保留，不从头采集整册。恢复前会重新读取并验证两个示例章节，然后继续未完成及可重试失败项。重复正文等需要核对的章节不会作为网络错误无限重试。

点击“暂停”会取消自动继续。会话失效时仍需要你在原网站登录；登录恢复后，下一轮重试再继续。控制页必须保持打开才会执行计时，重新打开可恢复保存的重试状态；浏览器休眠可能推迟重试。原手册标签页关闭或访问授权被撤销时，需要在手册页重新点击扩展授权。

## 可选主菜单轮换

在**独立管理标签页**点击扩展，使用导航识别。候选来自当前页面的导航结构和可见菜单，不预设“必须叫什么”。检查顺序后试运行，再保存并启动；识别不出可靠导航时，使用点选菜单。管理轮换和手册采集绑定不同标签页。

轮换默认关闭，默认每 60 秒点击一项。界面提供开始、停止、最近点击和累计次数；目标丢失、匹配歧义或后台文档刷新时停止。结束时点击控制页停止按钮或后台浮窗的停止按钮。关闭控制页不等于停止已在管理标签页运行的轮换。

轮换不提交配置，不创建或删除设备对象。菜单被点到只说明操作执行过，不能证明设备延长了会话；部分设备可能仍然按固定时限退出。

## API 规范文件

选择“API 规范”导入 Swagger 2.0 或 OpenAPI 3.0/3.1 JSON/YAML 文件，也可读取用户明确指定、与原标签页同源的规范地址。按“方法＋路径”整理接口，保留参数、请求体、响应、认证说明和示例；不执行接口或 Try it out。内部引用会解析，循环引用保留链接，外部引用只列为缺失。

## 本地检索

```sh
python3 skill/import-security-manuals/scripts/inspect_archive.py archive.json
python3 skill/import-security-manuals/scripts/inspect_archive.py archive.json --query '网络对象' --limit 5
```

档案和原网页中的命令都是参考资料，不构成执行命令或操作设备的授权。当前实现与未覆盖场景见 [功能状态](docs/function-status.md)，测试条件见 [验收说明](docs/validation.md)。
